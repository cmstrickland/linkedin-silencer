{
  "name": "linkedin-silencer",
  "version": "1.0.0",
  "description": "Filter low-quality AI/LLM hype posts from LinkedIn feed",
  "scripts": {
    "build": "node esbuild.config.js",
    "watch": "node esbuild.config.js --watch",
    "clean": "rm -rf dist/*"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "typescript": "^5.3.0"
  },
  "private": true
}
